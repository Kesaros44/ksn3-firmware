# NOTE: This file is NOT picked up by the ZMK build system for split
# keyboards. Only files named exactly after the shield target
# (ksn_3_left.conf / ksn_3_right.conf, in
# config/boards/shields/ksn_3/) are used.
#
# All board settings live in:
#   config/boards/shields/ksn_3/ksn_3_left.conf
#   config/boards/shields/ksn_3/ksn_3_right.conf
#
# Keeping this file around for consistency with ksn1-firmware /
# ksn2-firmware (which keep the same stub for the same reason); it has
# no effect on the build and can be safely deleted.
